{
  "name": "recipeaid-integration",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "cucumber-js --config cucumber.config.cjs",
    "test:headed": "cross-env PWDEBUG=1 cucumber-js --config cucumber.config.cjs",
    "test:feature": "cucumber-js --config cucumber.config.cjs --",
    "install:browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^11.0.1",
    "@types/node": "^24.0.0",
    "cross-env": "^7.0.3",
    "playwright": "^1.50.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
