# Default: auto-convert line endings on checkout
* text=auto

# Shell scripts must always use LF
*.sh text eol=lf

# Docker-related files must always use LF
Dockerfile text eol=lf
*.conf text eol=lf
entrypoint* text eol=lf
